apiVersion: kpt.dev/v1
kind: Kptfile
metadata:
  name: flux-helmrelease
  annotations:
    config.kubernetes.io/local-config: "true"
info:
  description: kpt package for provisioning a flux helmrelease CR
